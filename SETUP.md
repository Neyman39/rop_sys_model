# Настройка проекта для GitHub Pages

## ✅ Что уже настроено

1. **Vite конфигурация** - настроен `base: './'` для работы с относительными путями
2. **React Router** - используется HashRouter вместо BrowserRouter для корректной работы на GitHub Pages
3. **GitHub Actions workflow** - автоматический деплой при push в ветку `main`
4. **Цветовая схема** - добавлены красные акценты:
   - Активные разделы навигации (Контрагенты, Администрирование)
   - Кнопка "Вход" в хедере
   - Иконка на странице входа
   - Кнопка "Войти" на странице логина
   - Кнопка "Войти" на странице 401

## 🚀 Шаги для деплоя

### 1. Создайте репозиторий на GitHub

```bash
# Если еще нет git репозитория
git init
git add .
git commit -m "Initial commit: Реестр организаций и предпринимателей"
```

### 2. Подключите удаленный репозиторий

```bash
# Замените YOUR_USERNAME и YOUR_REPO на свои значения
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git branch -M main
git push -u origin main
```

### 3. Настройте GitHub Pages

1. Откройте репозиторий на GitHub
2. Перейдите в **Settings** → **Pages**
3. В разделе **Source** выберите **GitHub Actions**
4. Сохраните настройки

### 4. Дождитесь деплоя

1. Перейдите во вкладку **Actions**
2. Дождитесь завершения workflow "Deploy to GitHub Pages"
3. После успешного деплоя сайт будет доступен по адресу:
   ```
   https://YOUR_USERNAME.github.io/YOUR_REPO/
   ```

## 🔧 Локальная разработка

```bash
# Установка зависимостей (первый раз)
pnpm install

# Запуск dev сервера
pnpm run dev

# Открыть в браузере
# http://localhost:5173
```

## 📦 Ручная сборка

```bash
# Сборка проекта
pnpm run build

# Предварительный просмотр сборки
pnpm run preview
```

Папка `dist/` будет содержать готовую сборку для деплоя.

## 🎨 Примененные красные акценты

### Навигация
- Активная вкладка "Контрагенты" - красный фон
- Активная вкладка "Администрирование" - красный фон

### Кнопки входа
- Кнопка "Вход" в хедере - красный фон
- Кнопка "Войти" на странице логина - красный фон
- Кнопка "Войти" на странице 401 - красный фон

### Иконки
- Иконка здания на странице входа - красный фон

### Используемый цвет
```css
--destructive: #dc2626; /* red-600 */
```

## 📝 Структура файлов для деплоя

```
.github/
  workflows/
    deploy.yml          # GitHub Actions workflow

vite.config.ts          # Настроен base: './'
src/app/routes.tsx      # Используется createHashRouter
package.json            # Добавлены скрипты dev и preview

.gitignore              # Исключения для git
README.md               # Описание проекта
DEPLOYMENT.md           # Инструкции по деплою
USER_GUIDE.md           # Руководство пользователя
```

## ⚠️ Важные моменты

1. **HashRouter** - URL будут выглядеть как `/#/counterparties` вместо `/counterparties`
2. **Относительные пути** - все ассеты будут загружаться относительно текущего пути
3. **`.nojekyll`** - файл создается автоматически при сборке для корректной работы на GitHub Pages
4. **Автоматический деплой** - каждый push в `main` запускает пересборку и деплой

## 🆘 Решение проблем

### Сайт не открывается после деплоя
- Проверьте, что GitHub Pages включен в настройках
- Убедитесь, что выбран источник "GitHub Actions"
- Проверьте статус workflow в разделе Actions

### Ошибка 404 при переходе по страницам
- Убедитесь, что используется HashRouter
- Проверьте, что файл `.nojekyll` создан в dist

### Стили не применяются
- Проверьте console на ошибки загрузки CSS
- Убедитесь, что `base: './'` установлен в vite.config.ts

## 📞 Контакты

При возникновении вопросов создайте Issue в репозитории проекта.
